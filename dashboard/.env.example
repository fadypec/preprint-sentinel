# === Database ===
DATABASE_URL=postgresql://durc:durc_local@localhost:5432/durc_triage

# === NextAuth ===
AUTH_SECRET=generate-with-openssl-rand-base64-32

# GitHub OAuth (register at https://github.com/settings/applications/new)
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Google OAuth (optional, register at https://console.cloud.google.com/apis/credentials)
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=

# === Pipeline integration ===
# Override if .venv is in a non-standard location
# PIPELINE_PYTHON=/path/to/.venv/bin/python
