# Codebase Audit — 2026-04-01

## Critical — Fix Before Hosting Online

### 1. ~~No auth on API routes~~ FIXED (0c0e0d0)
Added `apiRequireAuth()` and `apiRequireAdmin()` guards to all 9 API routes. Read endpoints require auth; pipeline/settings mutations require admin role.

### 2. ~~Command injection risk in pipeline trigger~~ FIXED (0c0e0d0)
`fromDate`/`toDate` validated against `/^\d{4}-\d{2}-\d{2}$/` before passing to `spawn()`.

### 3. ~~CSP blocks inline script in paper-filters~~ FIXED (0c0e0d0)
Moved inline script to `public/filter-form.js`, loaded via `<script src>` (CSP-compliant with `script-src 'self'`).

### 4. ~~No Dockerfiles or deployment configuration~~ FIXED (0c0e0d0)
Created `pipeline/Dockerfile`, `dashboard/Dockerfile`, `.dockerignore` files, `docker-compose.prod.yml`, `dashboard/.env.example`.

---

## High — Fix Before Running a Month of Data

### 5. ~~LLM response validation missing~~ FIXED (0c0e0d0)
Added `_validate_result()` to coarse_filter (fail-open), methods_analysis, and adjudication. Checks required keys and enum validity. Invalid responses logged at warning level.

### 6. ~~No composite database index for hot query path~~ FIXED (0c0e0d0)
Added composite index `ix_papers_posted_date_stage` on `(posted_date, pipeline_stage)` in SQLAlchemy model, Prisma schema, and Alembic migration.

### 7. next-auth on beta version
`next-auth@5.0.0-beta.30` — pre-release. Accepted risk for now; will upgrade when 5.0 stable ships.

---

## Medium — Fix for Public Deployment

### 8. ~~No rate limiting on API routes~~ FIXED (11bc9ba)
Added in-memory sliding-window rate limiter (60 req/min per IP). Applied via auth guard layer.

### 9. ~~Settings API stores Slack webhooks in plaintext~~ FIXED (11bc9ba)
Settings GET now redacts secret keys. PUT preserves existing secrets if client sends the redacted placeholder.

### 10. ~~Dashboard .env.example missing~~ FIXED (0c0e0d0)
Created `dashboard/.env.example` with all required env vars documented.

### 11. ~~No CI/CD~~ FIXED (11bc9ba)
Added `.github/workflows/ci.yml`: Python (ruff lint + pytest) and Node (ESLint + tsc) on push/PR to main.

### 12. Dual ORM schema drift risk
SQLAlchemy and Prisma both define same schema independently. Mitigated by keeping both in sync manually. No Prisma migrations tracked yet.

---

## Low — Polish

- ~~No `output: "standalone"` in next.config.ts~~ FIXED (0c0e0d0)
- `findFirst` calls without `.take(1)` in a few places (low impact)
- ~~Overly broad `except Exception` in enrichment API clients~~ FIXED (11bc9ba)
- ~~Python deps use `>=` without upper bounds~~ FIXED (11bc9ba)
- No monitoring/observability (Sentry, OpenTelemetry) — deferred to post-launch
- ~~PDF parsing silently swallows errors~~ FIXED (11bc9ba) — now logs at warning level

---

## Remaining Items

- **next-auth beta** (#7): Accept risk or wait for stable 5.0
- **Dual ORM schema drift** (#12): Consider making one ORM the source of truth
- **Monitoring** (low): Add Sentry or similar after initial deployment
- **findFirst without .take(1)**: Minor, Prisma `findFirst` already limits to 1 row

---

## What's Good

- Pipeline architecture: 7 stages, error isolation per-paper, exponential backoff, structured logging
- No SQL injection: parameterized queries throughout (Prisma + SQLAlchemy)
- Secrets: Pydantic SecretStr, `.env` not in git
- Accessibility: semantic HTML, ARIA labels, `aria-live`, keyboard navigation
- Security headers: CSP, X-Frame-Options DENY, Permissions-Policy
- Cost design: Haiku bulk / Sonnet analysis / Opus adjudication only
- Skip-existing logic prevents re-processing on overlapping dates
- Prompt versioning + full audit trail on every LLM call
